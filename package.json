{
    "name": "radio-szkola-monorepo",
    "private": true,
    "scripts": {
        "build": "turbo run build && rm -rf ./dist && mkdir ./dist && cp -r ./apps/server/dist ./dist/server && cp -r ./apps/client/dist ./dist/client",
        "dev": "turbo run dev"
    },
    "packageManager": "pnpm@9.10.0",
    "devDependencies": {
        "concurrently": "^9.0.1",
        "prettier": "3.3.3",
        "turbo": "2.1.2"
    }
}
